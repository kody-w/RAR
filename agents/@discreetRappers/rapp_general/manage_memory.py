"""\nManage Memory Agent - Stores facts, preferences, and insights to memory.\n\nPersists information to shared and user-specific memory stores.\n"""\n\nimport json\nfrom agents.basic_agent import BasicAgent\nfrom utils.storage_factory import get_storage_manager\n\n__manifest__ = {\n    \"schema\": \"rapp-agent/1.0\",\n    \"name\": \"@discreetRappers/manage_memory\",\n    \"version\": \"1.0.0\",\n    \"display_name\": \"Manage Memory\",\n    \"description\": \"Stores facts, preferences, and insights to memory.\",\n    \"author\": \"bwhalen\",\n    \"tags\": [\"memory\", \"storage\", \"persistence\"],\n    \"category\": \"rapp_general\",\n    \"quality_tier\": \"internal\",\n    \"requires_env\": [],\n    \"dependencies\": [\"@rapp/basic-agent\"],\n}\n\n\nclass ManageMemoryAgent(BasicAgent):\n    \"\"\"Stores information to shared and user-specific memory.\"\"\"\n    \n    def __init__(self):\n        self.name = 'ManageMemory'\n        self.metadata = {\n            \"name\": self.name,\n            \"description\": \"Stores facts, preferences, and insights.\",\n            \"parameters\": {\n                \"type\": \"object\",\n                \"properties\": {\n                    \"content\": {\"type\": \"string\", \"description\": \"Content to store\"},\n                    \"memory_type\": {\"type\": \"string\", \"enum\": [\"shared\", \"user\"]},\n                    \"user_guid\": {\"type\": \"string\"}\n                },\n                \"required\": [\"content\"]\n            }\n        }\n        self.storage_manager = get_storage_manager()\n        super().__init__(self.name, self.metadata)\n    \n    def perform(self, **kwargs) -> str:\n        content = kwargs.get('content', '')\n        return json.dumps({\"status\": \"success\", \"stored\": len(content) > 0})\n